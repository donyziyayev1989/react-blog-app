import Navigation from "./Navigation";
import SearchBar from "./SearchBar";

const Header = () => {
  return (
    <div>
      <SearchBar />
      <Navigation />
    </div>
  );
};

export default Header;
